# Copy this to .env.local and fill in. .env.local is git-ignored.
# For GitHub Pages deploys, set VITE_RPC_URL via repo secret (Settings → Secrets).

VITE_RPC_URL=https://eth-mainnet.g.alchemy.com/v2/YOUR_KEY
VITE_AAVE_GRAPHQL=https://api.v4.aave.com/graphql
